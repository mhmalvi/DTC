<UserControl x:Class="DTCBillingSystem.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:DTCBillingSystem.UI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1024">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Welcome Message -->
            <TextBlock Grid.Row="0" 
                       Text="Dashboard Overview" 
                       Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                       Margin="0,0,0,24"/>

            <!-- Statistics Cards -->
            <WrapPanel Grid.Row="1" Margin="0,0,0,24">
                <!-- Total Customers Card -->
                <materialDesign:Card Width="280" Margin="8" Padding="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8">
                            <materialDesign:PackIcon Kind="AccountMultiple" 
                                                    Width="24" Height="24"
                                                    VerticalAlignment="Center"/>
                            <TextBlock Text="Total Customers" 
                                     Style="{StaticResource MaterialDesignBody1TextBlock}"
                                     Margin="8,0,0,0"
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <TextBlock Grid.Row="1" 
                                   Text="{Binding TotalCustomers}"
                                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                   Margin="8"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                </materialDesign:Card>

                <!-- Monthly Revenue Card -->
                <materialDesign:Card Width="280" Margin="8" Padding="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8">
                            <materialDesign:PackIcon Kind="CurrencyBdt" 
                                                    Width="24" Height="24"
                                                    VerticalAlignment="Center"/>
                            <TextBlock Text="Monthly Revenue" 
                                     Style="{StaticResource MaterialDesignBody1TextBlock}"
                                     Margin="8,0,0,0"
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <TextBlock Grid.Row="1" 
                                   Text="{Binding MonthlyRevenue, StringFormat='{}{0:N2} BDT'}"
                                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                   Margin="8"
                                   HorizontalAlignment="Right"/>
                    </Grid>
                </materialDesign:Card>

                <!-- Outstanding Amount Card -->
                <materialDesign:Card Width="280" Margin="8" Padding="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="8">
                            <materialDesign:PackIcon Kind="AlertCircle" 
                                                    Width="24" Height="24"
                                                    VerticalAlignment="Center"/>
                            <TextBlock Text="Outstanding Amount" 
                                     Style="{StaticResource MaterialDesignBody1TextBlock}"
                                     Margin="8,0,0,0"
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                        
                        <TextBlock Grid.Row="1" 
                                   Text="{Binding OutstandingAmount, StringFormat='{}{0:N2} BDT'}"
                                   Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                                   Margin="8"
                                   HorizontalAlignment="Right"
                                   Foreground="{StaticResource MaterialDesignErrorLight}"/>
                    </Grid>
                </materialDesign:Card>
            </WrapPanel>

            <!-- Recent Activity and Charts -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Recent Activity List -->
                <materialDesign:Card Grid.Column="0" Margin="8" Padding="16">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" 
                                   Text="Recent Activity"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Margin="0,0,0,16"/>
                        
                        <ListView ItemsSource="{Binding RecentActivities}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <materialDesign:PackIcon Grid.Column="0"
                                                                Kind="{Binding Icon}"
                                                                Width="24" Height="24"
                                                                Margin="0,0,16,0"
                                                                VerticalAlignment="Center"/>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Description}"
                                                     Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                                            <TextBlock Text="{Binding UserName}"
                                                     Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                     Opacity="0.6"/>
                                        </StackPanel>

                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding Timestamp, StringFormat='{}{0:g}'}"
                                                   Style="{StaticResource MaterialDesignCaptionTextBlock}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </DockPanel>
                </materialDesign:Card>

                <!-- Quick Actions -->
                <StackPanel Grid.Column="1" Width="200" Margin="0,8,8,8">
                    <materialDesign:Card Padding="16" Margin="0,0,0,8">
                        <StackPanel>
                            <TextBlock Text="Quick Actions"
                                     Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                     Margin="0,0,0,16"/>
                            
                            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                    Command="{Binding NewBillCommand}"
                                    Margin="0,4">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileDocumentPlus" Margin="0,0,8,0"/>
                                    <TextBlock Text="New Bill"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                    Command="{Binding RecordPaymentCommand}"
                                    Margin="0,4">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="CashPlus" Margin="0,0,8,0"/>
                                    <TextBlock Text="Record Payment"/>
                                </StackPanel>
                            </Button>

                            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                    Command="{Binding GenerateReportCommand}"
                                    Margin="0,4">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="FileChart" Margin="0,0,8,0"/>
                                    <TextBlock Text="Generate Report"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </materialDesign:Card>

                    <materialDesign:Card Padding="16">
                        <StackPanel>
                            <TextBlock Text="System Status"
                                     Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                     Margin="0,0,0,16"/>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <materialDesign:PackIcon Kind="Database" Margin="0,0,8,0"/>
                                <TextBlock Text="Last Backup:"/>
                                <TextBlock Text="{Binding LastBackupTime, StringFormat='{}{0:g}'}"
                                         Margin="8,0,0,0"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <materialDesign:PackIcon Kind="Sync" Margin="0,0,8,0"/>
                                <TextBlock Text="Last Sync:"/>
                                <TextBlock Text="{Binding LastSyncTime, StringFormat='{}{0:g}'}"
                                         Margin="8,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </materialDesign:Card>
                </StackPanel>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl> 