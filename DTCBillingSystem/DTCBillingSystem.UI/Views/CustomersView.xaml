<UserControl x:Class="DTCBillingSystem.UI.Views.CustomersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DTCBillingSystem.UI.Views"
        xmlns:vm="clr-namespace:DTCBillingSystem.UI.ViewModels"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        d:DesignHeight="600" d:DesignWidth="1000">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Customers" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Add Customer" Command="{Binding AddCustomerCommand}" 
                    Margin="20,0,0,0" Padding="10,5"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                    Margin="10,0,0,0" Padding="10,5"/>
        </StackPanel>

        <!-- Filters -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Search customers..."
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,0,10,0"/>

            <ComboBox Grid.Column="1" ItemsSource="{Binding Source={StaticResource CustomerTypes}}"
                      SelectedItem="{Binding SelectedCustomerType}"
                      Width="150" Margin="0,0,10,0"/>

            <ComboBox Grid.Column="2" ItemsSource="{Binding Source={StaticResource SortOptions}}"
                      SelectedItem="{Binding SortBy}"
                      Width="150" Margin="0,0,10,0"/>

            <CheckBox Grid.Column="3" Content="Active Only" 
                      IsChecked="{Binding IsActive}"/>
        </Grid>

        <!-- Customer List -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Customers}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" SelectionUnit="FullRow"
                  CanUserSortColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Account #" Binding="{Binding AccountNumber}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
                <DataGridTextColumn Header="Zone" Binding="{Binding ZoneCode}"/>
                <DataGridTextColumn Header="Contact" Binding="{Binding ContactNumber}"/>
                <DataGridTextColumn Header="Balance" Binding="{Binding CurrentBalance, StringFormat=C2}"/>
                <DataGridTextColumn Header="Status" Binding="{Binding IsActive, Converter={StaticResource BoolToStatusConverter}}"/>
                <DataGridTemplateColumn Header="Actions">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Edit" 
                                        Command="{Binding DataContext.EditCustomerCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0"/>
                                <Button Content="View Bills" 
                                        Command="{Binding DataContext.ViewBillsCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0"/>
                                <Button Content="{Binding IsActive, Converter={StaticResource BoolToActionTextConverter}}"
                                        Command="{Binding DataContext.ActivateDeactivateCommand, 
                                        RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Previous" Command="{Binding PreviousPageCommand}"
                    IsEnabled="{Binding CanGoToPreviousPage}"/>
            <TextBlock Text="{Binding CurrentPage}" Margin="10,0"/>
            <TextBlock Text="of" Margin="5,0"/>
            <TextBlock Text="{Binding TotalPages}" Margin="10,0"/>
            <Button Content="Next" Command="{Binding NextPageCommand}"
                    IsEnabled="{Binding CanGoToNextPage}"/>
            <ComboBox ItemsSource="{Binding PageSizes}" 
                      SelectedItem="{Binding ItemsPerPage}"
                      Width="70" Margin="20,0,0,0"/>
        </StackPanel>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="4" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <ProgressBar IsIndeterminate="True" Width="100" Height="100"/>
        </Grid>
    </Grid>
</UserControl> 