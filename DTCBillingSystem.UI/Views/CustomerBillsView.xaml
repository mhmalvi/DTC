<Page x:Class="DTCBillingSystem.UI.Views.CustomerBillsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:DTCBillingSystem.UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Customer Bills">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="{Binding CustomerName}" 
                           Style="{StaticResource MaterialDesignHeadline5TextBlock}" 
                           Margin="0,0,0,8"/>
                <TextBlock Text="{Binding CustomerDetails}" 
                           Style="{StaticResource MaterialDesignBody1TextBlock}" 
                           Opacity="0.6"/>
            </StackPanel>

            <Button Grid.Column="1"
                    Content="Back"
                    Command="{Binding BackCommand}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </Grid>

        <!-- Bills List -->
        <materialDesign:Card Grid.Row="1">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Filters -->
                <StackPanel Grid.Row="0" 
                           Orientation="Horizontal" 
                           Margin="0,0,0,16">
                    <ComboBox Width="150"
                              materialDesign:HintAssist.Hint="Bill Status"
                              ItemsSource="{Binding BillStatuses}"
                              SelectedItem="{Binding SelectedBillStatus}"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"/>

                    <DatePicker Width="150"
                                Margin="16,0,0,0"
                                materialDesign:HintAssist.Hint="From Date"
                                SelectedDate="{Binding FromDate}"
                                Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>

                    <DatePicker Width="150"
                                Margin="16,0,0,0"
                                materialDesign:HintAssist.Hint="To Date"
                                SelectedDate="{Binding ToDate}"
                                Style="{StaticResource MaterialDesignOutlinedDatePicker}"/>
                </StackPanel>

                <!-- Bills DataGrid -->
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding Bills}"
                          SelectedItem="{Binding SelectedBill}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding BillNumber}"
                                           Header="Bill No."
                                           Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding BillingMonth, StringFormat='{}{0:MMM yyyy}'}"
                                           Header="Month"
                                           Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Amount, StringFormat='₹ {0:N2}'}"
                                           Header="Amount"
                                           Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding Status}"
                                           Header="Status"
                                           Width="Auto"/>
                        <DataGridTextColumn Binding="{Binding DueDate, StringFormat='{}{0:dd MMM yyyy}'}"
                                           Header="Due Date"
                                           Width="Auto"/>
                        <DataGridTemplateColumn Width="Auto">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Command="{Binding DataContext.ViewBillCommand, 
                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                ToolTip="View Bill">
                                            <materialDesign:PackIcon Kind="Eye"/>
                                        </Button>
                                        <Button Command="{Binding DataContext.PrintBillCommand, 
                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                ToolTip="Print Bill">
                                            <materialDesign:PackIcon Kind="Printer"/>
                                        </Button>
                                        <Button Command="{Binding DataContext.RecordPaymentCommand, 
                                                RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource MaterialDesignIconButton}"
                                                ToolTip="Record Payment">
                                            <materialDesign:PackIcon Kind="CashRegister"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</Page> 